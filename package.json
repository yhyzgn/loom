{
  "name": "@neo/loom",
  "version": "0.1.0",
  "description": "Cross-harness agent configuration sync CLI.",
  "type": "module",
  "bin": {
    "loom": "./bin/loom"
  },
  "files": [
    "bin/loom",
    "config",
    "agents",
    "shared",
    "shims",
    "scripts",
    "templates",
    "README.md"
  ],
  "scripts": {
    "doctor": "./bin/loom doctor",
    "test": "python3 - <<'PY'\nimport ast; ast.parse(open('bin/loom', encoding='utf-8').read()); print('syntax ok')\nPY\n./bin/loom doctor"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true
}
